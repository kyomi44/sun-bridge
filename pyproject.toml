[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "open-permit-kit"
version = "0.1.0"
description = "Operator-guided tools for understanding permit notifications and reconciling CRM records"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
dependencies = []

[tool.setuptools]
packages = ["permitkit"]
