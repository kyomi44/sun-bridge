[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "sun-bridge"
version = "0.3.0"
description = "Open AHJ and utility data, historical solar timelines, and reviewable CRM workflows"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
dependencies = []

[tool.setuptools]
packages = ["sunbridge"]
