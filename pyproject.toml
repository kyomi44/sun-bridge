[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "sun-bridge"
version = "0.2.0"
description = "People-first learning and review tools for increasingly automated solar operations"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
dependencies = []

[tool.setuptools]
packages = ["permitkit", "solarbridge", "sunbridge"]
